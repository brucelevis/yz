--/*
-- 所有服务器配置信息，保证和配置文件中填写一样，自身配置优先读取配置文件
-- 获取其他服信息才读取这里的配置
--*/
srvlist = {
	datacenter = {
		ip = "127.0.0.1",
		port = 9000,
		db = {
			host = "127.0.0.1",
			port = 6379,
			db = 11,
		},
	},
	gamesrv_100 = {
		ip = "127.0.0.1",
		port = 8080,
		minroleid = 1000000,
		maxroleid = 2000000,
		maxclient = 10240,
        srvno = 100,
		db = {
			host = "127.0.0.1",
			port = 6379,
			db = 0,
		},
	},
}

return srvlist

